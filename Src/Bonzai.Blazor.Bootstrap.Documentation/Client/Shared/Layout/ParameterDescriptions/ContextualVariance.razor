<ParameterDescription Name="Variance"
                      Type="typeof(string)">
    Provides visual context for the component. Bootstrap defines the following values @Variants<text>.</text>
</ParameterDescription>

@code {
    private string Variants => string.Join(", ", BootstrapVariations.ContextualVariations);
}
