@inherits BootstrapVariantBase

<img @attributes="AdditionalAttributes" class="@Classes" />

@code {
    protected override string DefaultClass => $"card-image-{Variance}";

    public CardImage() : base("top") { }
}
